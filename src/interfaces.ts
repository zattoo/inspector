export interface Configuration {
    /**
     * List of terms the script should ignore
     */
    ignore?: string[];

    /**
     * Path to the folder that should be tested
     */
    path?: string;
}
